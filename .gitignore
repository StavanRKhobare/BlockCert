# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
env/
.env
.ENV
*.egg-info/
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/

# Node / Vite
node_modules/
dist/
dist-ssr/
*.local
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Checkpoint blobs (local off-chain store — ephemeral per gateway run)
checkpoint_data/
gateway/checkpoint_data/
rollback-service/checkpoint_data/

# Hardhat / chain artifacts — volatile (rewritten each deploy:localhost)
# Kept tracked for now (addresses needed), but their cache is ignored:
blockchain/cache/
blockchain/artifacts/
blockchain/typechain-types/

# IDE / OS
.vscode/
.idea/
.DS_Store
*.suo
*.swp
*.swo
*~

# Project large blobs kept tracked in CONTEXT/ — ignore if duplicated at root
# (legacy deletions already handled; this prevents re-adding a root copy)
# context.md at root is deleted in favor of CONTEXT/context.md — do not re-add
