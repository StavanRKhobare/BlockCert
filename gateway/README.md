# gateway

FastAPI service that wires fl-orchestrator, auth-service, rollback-service,
attribution-service, blockchain/client, and provenance-api together behind
one API surface and drives the round lifecycle end to end.

## Build last (of the non-vision modules)
Once modules 1-7 (see root README) each work standalone against mocks,
wire them here. This is also what dashboard/ talks to. Because everything
below it is already interface-driven, this can be fully built and
demoed end-to-end BEFORE the real vision model exists.

Continuous Hardhat node: unlike prior epics' per-story fresh nodes, this
epic keeps ONE `npx hardhat node` running (blockchain/) for all stories —
gateway state (DIDs, stakes, round history) accumulates across stories.

## Build Log

- [G0] gateway project setup (own `.venv`; requirements fastapi/uvicorn/web3/pydantic/pytest/httpx + numpy/scikit-learn per root requirements; trivial FastAPI `main.py` with GET /health) — files: `gateway/requirements.txt`, `gateway/main.py`, `gateway/tests/__init__.py`, `gateway/tests/conftest.py`, `gateway/tests/test_g0_setup.py`. Notes: (a) conftest inserts repo root + all five sibling dirs + gateway/ (same pattern as fl-orchestrator's conftest); auth-service's `entrypoint` and rollback-service's `chain_bridge` win plain imports — provenance-api's `entrypoint` loads by file location with sys.modules save/restore (both services ship those top-level names; new ambiguity, documented in-test); (b) httpx 0.28 removed TestClient and its ASGITransport is async-only, so the health test drives the app via AsyncClient inside asyncio.run — still httpx, no live server.
- [G1] GatewaySession — explicit state, no module-level globals — files: `gateway/session.py`, `gateway/tests/test_session.py`. Notes: (a) every run_round call passes previous_checkpoint + checkpoint_registry from self; session.py references zero round_manager globals in code (only `run_round` imported — grep-verified, mentions elsewhere are docstring); (b) THREE prompt-vs-reality gaps, all documented in session.py's module docstring instead of worked around silently: run_round takes NO checkpoint_store param (session owns one for gateway-side reads; run_round persists via its internal default — residual, flagged upstream); the summary carries NO Checkpoint so the new head is reconstructed from session-owned registry + summary (hashes/URIs/parents real; is_delta/metrics mirror hasher hardcodes with source refs; timestamp fresh); make_clients returns adapters not SimulatedClients (wrapped exactly like the C-tests); (c) owned DriftMonitor held per design but run_round ingests into its own internal monitor (same residue class — no monitor param exists); (d) test: 3 real rounds via asyncio.run (8/8 pass, no drift), round==3, history==3, idle between calls, 3 distinct head hashes with parent links, own registry mutated (len 3) while round_manager._default_registry stays empty.
- [G2] DID registration + client registry endpoint — files: `gateway/chain_clients/__init__.py`, `gateway/chain_clients/did_client.py` (DIDClient + StakingClient, one small file per prompt's call), `gateway/routers/__init__.py`, `gateway/routers/clients.py` (GET /clients), `gateway/session.py` (+register_all_clients, client_stats, chain_dids), `gateway/main.py` (wires router), `gateway/tests/test_clients_router.py`. Notes: (a) exact Solidity signatures honored — registerDID(did,publicKey,displayName), stake(did) payable (amount via msg.value), balanceOf view; wrappers return tx-hash hex; (b) DIDs did:bfa:client-{i}, dummy pubkeys flagged as known gap (no keypair infra), 1 ETH-wei initial stake flagged arbitrary placeholder; (c) registration idempotent (skip-if-registered, stake-only-if-zero) and rerun/redeploy-safe; eager at construction when no running loop, otherwise routers call it first (documented); (d) RENAMED gateway/chain_bridge → gateway/chain_clients mid-story: creating that dir shadowed rollback-service's top-level `chain_bridge` for plain imports and broke G0 — the rename (documented here) restores G0 green with zero importlib hacks; (e) rounds_passed advances only on clean (n_failed==0) rounds — per-client attribution is impossible from summary totals, so counts hold on dirty rounds instead of guessing (flagged for a future per-client-verdicts return); (f) router session is lazy singleton + set_session() injection so importing main never needs the chain; (g) test is live against the continuous node: 8 entries, balances == INITIAL_STAKE_WEI, chain re-read agrees.
- [G3] Manual round-step endpoint with attacker scenario — files: `gateway/session.py` (+C5 arming, ATTACK_START_ROUND/ATTACK_CLIENT_INDEX), `gateway/routers/rounds.py` (POST /rounds/step, GET /rounds/current, GET /rounds/history), `gateway/main.py` (wires router), `gateway/tests/test_rounds_step.py`. Notes: (a) C5's flag-patching pattern copied verbatim (embed poisoned + run_round drifted via armed-flag closures, flipped at round 12) — no reinvention; (b) test steps 15 real rounds live: 1-11 all pass, attacker excluded EVERY round 12-15 (stronger than C5's any-round finding, same mechanism); current/history endpoints verified; (c) G1's 3-round test unaffected (arming starts at 12).
- [G4] Autonomous round loop — files: `gateway/routers/rounds.py` (+autonomous start/stop/status), `gateway/tests/test_rounds_autonomous.py`. Notes: (a) background asyncio.Task on session.autonomous_task, interval sleep between rounds, auto-stops at max_rounds; duplicate start → 409, stop cancels cooperatively (lands at next await) and status retrieves finished-task exceptions so nothing warns; (b) NO run_in_threadpool, deliberately: measured ~2.7s/round is 2.4s of yielding pacing sleeps + ms-scale compute slices, so the loop stays responsive — revisit once the vision model changes step_round's blocking profile; (c) test runs the whole scenario in ONE shared event loop (per-request asyncio.run would strand the task on a closed loop): start → 409 on duplicate → 3.5s wait advances ≥1 round with zero manual steps → stop freezes the round.

- [G0] gateway project setup (own `.venv`; requirements fastapi/uvicorn/web3/pydantic/pytest/httpx + numpy/scikit-learn per root requirements; trivial FastAPI `main.py` with GET /health) — files: `gateway/requirements.txt`, `gateway/main.py`, `gateway/tests/__init__.py`, `gateway/tests/conftest.py`, `gateway/tests/test_g0_setup.py`. Notes: (a) conftest inserts repo root + all five sibling dirs + gateway/ (same pattern as fl-orchestrator's conftest); auth-service's `entrypoint` and rollback-service's `chain_bridge` win plain imports — provenance-api's `entrypoint` loads by file location with sys.modules save/restore (both services ship those top-level names; new ambiguity, documented in-test); (b) httpx 0.28 removed TestClient and its ASGITransport is async-only, so the health test drives the app via AsyncClient inside asyncio.run — still httpx, no live server.
