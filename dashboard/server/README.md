# dashboard-server

Server Dashboard (Epic SD) — fleet-wide operator view: system pipeline map,
per-client suspicion heatmap, FedAvg aggregation visualizer, drift-monitor
gate + checkpoint hash-chain graph, blockchain explorer (transactions,
disputes, staking economics), provenance ledger, attribution placeholder.

Separate app from `dashboard/client/` (Epic CD) by design — own
package.json, own node_modules, no imports from the client app. Small
shared bits (types, Legend, TeacherModeToggle) are deliberately duplicated
per copy, same philosophy as `passport_client.py` vs. rollback-service's
`chain_client.py`: changing one app never risks breaking the other.

Consistency contract with the client app: both dashboards derive from
identical underlying scenario numbers — the real B4.5 calibrated threshold
(~9.98), the measured 8-client honest-band scores, client-3 turning
poisoned (~25-26) from round 12 onward. See `src/mock/serverFeed.ts`
(SD2) for provenance notes.

## Build Log

- [SD0] dashboard-server project setup (scaffolded at `dashboard/server/` — Vite react-ts, Tailwind v3 pinned, recharts + @xyflow/react + framer-motion, Vitest + RTL + jsdom; `vite.config.ts` test env jsdom + globals; `src/test/setup.ts` ResizeObserver stub carried over from CD for xyflow-in-jsdom; Tailwind directives at top level — CD0's media-query scoping bug not repeated) — files: `dashboard/server/` scaffold + config (`package.json`, `vite.config.ts`, `tailwind.config.js`, `postcss.config.js`, `src/test/setup.ts`, `src/index.css`, `src/App.test.tsx`). Note: deviates from prompt's top-level `dashboard-server/` path the same way CD0 did (`dashboard/client/` precedent).
- [SD1] TypeScript types (7 backend mirrors copied field-for-field from CD1 + 4 server-only types) — files: `dashboard/server/src/types/schemas.ts`, `dashboard/server/src/types/schemas.test.ts`. Notes: (a) CD1's per-client `RoundPipelineStage` intentionally NOT copied — this app uses system-wide `SystemStage` instead; (b) `TransactionRecord`/`DisputeStatus`/`Dispute` flagged in-file as explorer/demo-only with no backend mirror; (c) same Vitest-needs-one-`it` workaround as CD1 — real check is `tsc --noEmit`.
- [SD2] Mock server-wide feed + useServerFeed hook (single data source; slices expose only completed rounds; ServerFeedProvider for shared app state, local fallback outside it) — files: `dashboard/server/src/mock/serverFeed.ts`, `dashboard/server/src/mock/useServerFeed.ts`, `dashboard/server/src/mock/useServerFeed.test.ts`. Notes: (a) all 8 clients scored per round from CD's verbatim measured tuples (client-3's scores identical to the client app — verified by literal diff: all 17 score tuples + 20 checkpoint hashes present); the spec's parenthetical honest-band list ([8.9265 …]) is NOT what CD5 used (recorded non-reproducing in CD's build log), so CD5's real numbers were copied instead — the side-by-side requirement; (b) client-3's staked@1/slashed@12 verbatim, other 7 staked@1 uniform filler flagged as such; (c) tx hashes/gas/blocks fabricated demo filler (deterministic LCG, no randomness); (d) ONE // SYNTHETIC DriftEvent @17 (window 13-17, reverts to round-11 anchor), real D3 monitor still untriggerable on placeholder accuracy; (e) disputes as 3 status snapshots of dispute_id 1 visible @12/13/15; (f) 8-step stage cycle, `ServerStage = SystemStage | "idle"` (idle is a feed concern, SD1 has no idle member).
- [SD3] Living system map (xyflow, 6 architecture layers, framer-motion pulse on the stage-owning layer, click-to-expand + anchor scroll) — files: `dashboard/server/src/components/SystemMap.tsx`, `dashboard/server/src/components/SystemMap.test.tsx`, `dashboard/server/src/index.css` (+sys-node theme + teacher-mode copy). Notes: (a) pure `layerForStage()` mapping asserted directly + rendered highlights sampled; (b) attribution node grayed/non-interactive (SD10 territory, clicks ignored); (c) panel anchors (`panel-auth`, `panel-aggregation`, `panel-rollback`, `panel-blockchain`, `panel-attribution`, `panel-provenance`) reserved for SD4-SD9 to adopt — scroll is a guarded no-op until they land; (d) active glow amber per shared legend (in-progress = pending), same visual language as CD3.
- [SD4] Suspicion heatmap (plain divs in CSS grid — no heatmap library; 3-bucket green→amber→red scale vs CALIBRATED_THRESHOLD, exact-score title tooltips) — files: `dashboard/server/src/components/SuspicionHeatmap.tsx`, `dashboard/server/src/components/SuspicionHeatmap.test.tsx`. Notes: (a) pure `heatBucket()` asserted directly; (b) test at round 20 over REAL feed data — client-3 r12-20 all `heat-over`, all 151 other cells `heat-under`; (c) adopts the `panel-auth` anchor reserved in SD3.
- [SD5] FedAvg aggregation visualizer (xyflow, 8 client nodes → global node, animated contributor edges vs dimmed/dashed excluded edges, centralized-training contrast toggle) — files: `dashboard/server/src/components/FedAvgVisualizer.tsx`, `dashboard/server/src/components/FedAvgVisualizer.test.tsx`; revised `dashboard/server/src/test/setup.ts` (ResizeObserver stub now reports fixed dimensions — a no-op stub was found to suppress xyflow edge geometry entirely in jsdom). Notes: (a) pure `buildFedAvgEdges()` asserted per edge id + rendered identity list ties client-3 to the dimmed edge — edge SVG itself unassertable in jsdom (zero-size viewport omits edges from DOM; asserting library internals there would be fake, documented in-file); (b) test at round 15 over REAL feed data; (c) adopts the `panel-aggregation` anchor reserved in SD3.
- [SD6] Drift monitor AND-gate + checkpoint hash-chain graph — files: `dashboard/server/src/components/DriftMonitorGate.tsx`, `dashboard/server/src/components/DriftMonitorGate.test.tsx`, `dashboard/server/src/components/CheckpointChainGraph.tsx`, `dashboard/server/src/components/CheckpointChainGraph.test.tsx`. Notes: (a) pure `evaluateDriftGate()`/`buildRoundSignals()` replay D3's exact arithmetic (window 5, mean > 2.0 AND accuracy trend < 0, full-window required) — halves light from computation (suspicion on ~round 5, accuracy on ~round 12 in this scenario), never hardcoded; (b) ROLLBACK gated on halves AND synthetic-event presence (coincides with round >= 17 here, but the event — not the round number — is the trigger); (c) pure `buildCheckpointChain()` resolves forward edges via parent_hash with previous-round fallback; revert edge (red dashed, labeled "synthetic revert (demo)") runs latest → round-11 anchor only at 17+; (d) honesty copy enforced in UI: stand-in notes on both panels naming the synthetic event as demo-only; (e) gate adopts the `panel-rollback` anchor reserved in SD3.
- [SD7] Blockchain explorer + dispute kanban — files: `dashboard/server/src/components/BlockchainExplorer.tsx`, `dashboard/server/src/components/BlockchainExplorer.test.tsx`, `dashboard/server/src/components/DisputeKanban.tsx`, `dashboard/server/src/components/DisputeKanban.test.tsx`. Notes: (a) pure `computeStakeBalances()` (staked/restored +, slashed −) asserted against recomputed mock totals (client-3 75, rest 100 @20); tx feed newest-first + head-ticker also asserted; (b) kanban shows latest snapshot per dispute_id (single gliding card, framer-motion layout spring), pure `columnForStatus()`/`latestSnapshotPerDispute()` asserted; card column verified at rounds 11 (empty) → 12 provisional → 13 challenge → 15/20 finalized; (c) explorer adopts the `panel-blockchain` anchor reserved in SD3; kanban adds `panel-disputes`.
- [SD8] Staking economics (fleet total, per-round slash bars, 1-of-8 security-budget framing) — files: `dashboard/server/src/components/StakingEconomics.tsx`, `dashboard/server/src/components/StakingEconomics.test.tsx`. Notes: (a) total reuses SD7's pure `computeStakeBalances()` (sibling import, same CD precedent as DeviceLookup/PassportRow) — 775.0 @20 asserted; (b) pure `buildSlashSeries()` asserted (zeros except 25 @12); (c) budget line derived only from demonstrated numbers (1/8 clients = 12.5%, ≈1/8 of stake) — no invented percentages; (d) ResponsiveContainer seam mocked in jsdom exactly like CD4's test.
- [SD9] System-wide provenance ledger (ALL devices, newest first — the aggregate counterpart to CD8's single-device search) — files: `dashboard/server/src/components/ProvenanceLedger.tsx`, `dashboard/server/src/components/ProvenanceLedger.test.tsx`; extended `dashboard/server/src/mock/serverFeed.ts` (+PASSPORT_ENTRIES/+PASSPORT_ENTRY_ROUNDS) and `dashboard/server/src/mock/useServerFeed.ts` (+passportEntries slice, CD7-pattern extension). Notes: (a) 3 device-7 entries verbatim from the client app (same device/hashes/rounds/actor — side-by-side consistency); device-12/device-21 entries flagged as structural filler; (b) test over REAL feed data: 5 rows, 3 distinct devices, newest-first order asserted; (c) adopts the `panel-provenance` anchor reserved in SD3.
- [SD10] Culprit attribution placeholder (grayed, non-interactive, standing on-hold decision) — files: `dashboard/server/src/components/AttributionPlaceholder.tsx`, `dashboard/server/src/components/AttributionPlaceholder.test.tsx`. Notes: (a) reserves the `panel-attribution` anchor and mirrors SD3's grayed map node; (b) test asserts the label plus zero interactive elements (button/a/input/select/textarea/role=button).
- [SD-G11] Swap dashboard-server from mock to real gateway data (G11) — files: `dashboard/server/src/mock/useServerFeed.ts` (rewritten as live polling, same hook name/shape), `dashboard/server/src/mock/useServerFeed.test.ts` (fetch-mocked rewrite), `dashboard/server/src/App.test.tsx` (fetch-mocked live-feed version + combined SD-G11 verdict), `dashboard/server/.env` (VITE_GATEWAY_URL, default :8000). Notes: (a) LIVE: round/stage from GET /rounds/current (already SystemStage vocabulary — passthrough, unknown→idle stated), isPlaying from autonomous/status, play/pause/step POSTs, clientDids from GET /clients (chain did:bfa:…), transactions passed through field-identical, passportEntries from the NEW GET /passport/all (unindexed by round — entries carry no round field, documented); reset() honest no-op with the verbatim warning; (b) STOP-AND-SAY GAPS: NO scores/checkpoint/stake/dispute endpoints exist, so those 4 arrays stay on mock replay sliced by the LIVE round; the round-17 DriftEvent stays SYNTHETIC by explicit requirement (real monitor still placeholder-bound, G8) — mock data + synthetic, live counter, loudly labeled (header + additive dataSource discriminator; 14 downstream keys byte-identical); (c) KNOWN CONSEQUENCE: replay identities (did:example:…) no longer join live did:bfa:… rows (e.g. heatmap cells show unscored until a scores endpoint lands) — documented here, not patched over; (d) tests mock fetch (G10's documented option); verified: tsc, full suite 13 files/16 tests green, `npm run build` green.
- [SD11] App shell, teacher mode, shared legend (own Legend/TeacherModeToggle copies — identical green/red/amber scheme, no cross-app imports) — files: `dashboard/server/src/components/Legend.tsx`, `dashboard/server/src/components/TeacherModeToggle.tsx`, `dashboard/server/src/App.tsx`, `dashboard/server/src/App.test.tsx`; one-line fix in `dashboard/server/src/mock/serverFeed.ts` (unused forEach param). Notes: (a) all SD3-SD10 panels + play/pause/step/reset + round/stage readout under one ServerFeedProvider; (b) test with fake timers: 16/16 testids present, play + 8×800ms → round 1 in controls AND heatmap, pause freezes it; (c) `npm run build` (tsc -b) caught an unused-var error that `npx tsc --noEmit` missed — build is the stricter gate here, now green (chunk-size warning only, same benign one as the client app).
